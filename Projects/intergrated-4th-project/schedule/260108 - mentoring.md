1. 일기예보 같은 데이터는 시간이 지나면 쓸모가 없어지므로 캐시 형식으로 저장해놓는 것이 이상적임. 만약 Redis를 바로 붙이지 못할 경우 DB나 ConcurrentHashMap 등을 활용해 기능을 구현하고 추후 확장하는 것이 좋은지?
    
    1. Heap에 저장하는 것으로 보임. 제일 쉬운것은 RDB 저장.
        
    2. 데이터 삭제 유효성 검사로 돌리는 것이 좋아보임.
        
    3. api 쏘게 되면 그때 체크해서 삭제해도 무방함.
        
    4. 힙에다 저장하는게 좋다고 생각했나? → 메모리에 계속 남게되면 위험함. OOM 발생 가능 유효성 체크에 자신이 있다면 시도해도 좋다. 재시작 이후에 처리하는 건? 어떻게 처리할 건지?
        
    5. 동시성 문제라던가 여러가지 복잡한 문제가 굉장히 많기 때문에 본인같으면 바로 Redis를 바로 붙였음.
        
2. 현재 시계열 데이터를 구분하기 위해 데이터 수집 시간과 시계열 데이터를 저장해야하는 대상을 FK로 이용해 복합 PK 사용 중. JPA로 사용해보니 생각보다 복잡해지는 상황. N+1 문제 때문에 native Query도 사용하게 될 것 같은데 대체키 사용하는 것과 복합 PK 사용 중 어떤 것이 코드 작성 관점이나 성능 면에서 유리한지 비교하고 싶다.  
    PK는 내부 클래스로 해주면 좋을 듯. 안 할 이유가 있나?  
    분 단위 저장됨
    
    1. 복합키가 어떤 것이냐에 따라 다름. 성능 생각해야 함. 복합키 자체가 길어지게 되면 성능 상 이슈 존재.  
        JPA를 많이 안써보고 myBatis의 native한 쿼리를 많이 써봤다.  
        millisec까지 꼭 표현을 해야하나?  
        밀리초 절삭을 해서 넣으시오. → DATE_TRUNC()를 이용해 테이블 정보 절삭 가능  
        최대한 복합키 크기를 줄여보시오.
        
    2. 나중에 복합키 이용해서 처리할 때 콘솔 쪽에서 조회하면 JPA 쿼리 로그 찍힐 것임. 복합키 쿼리  
        만약 JPA 자체에서 조회 시 느릴 수 있어서 native Query로 개선할 수 있다.
        
    3. best Practice에서도 JPA에서 복합키를 클래스로 분리하는 식으로 사용하라고 권장함. 더 찾아보면 좋을 듯?
        

멘토 피드백

- areacode(외부 값을) Pk로 사용하는 것은 위험할 수 있다. → 나중에 보완해보길
    

API의 데이터를 그대로 사용하면 문제가 될 수 있음.

데이터 규격 변경 시 (PK) 미제공 시, 어떻게 할 건지?

갑자기 미제공 되더라도 작동할 수 있도록 별도 PK로 저장시키는 것이 이상적이다.

외부 데이터의 제어가 불가능하므로, 이렇게 하는 것이 좋은데 areaCode 기반으로 조회하고 설정하는 것이 굉장히 많이 작업 되어있어서 일단 이렇게 피드백 주었다.

→ 따라서 현 상태에서 대안은 없지만 외부 API 데이터의 PK를 그대로 가져오는 건 위험하다.

코드 작동 정렬 있으니 사용해볼 것

Swagger

- body에서 required, 필수 여부 작성 하는 것들이 빠져있던 것이 존재하다.
    

요청에 대한 필수, 아닌지, nullable 한 건지 아닌지 보여주는 것이 필요해서 말 꺼냄

- Wrapper 클래스 많이 사용하는 것으로 보임.
    

기본값이 1이라는 것은 null 을 받지 않겠다는 것인데, 이런 것은 원시형을 사용해볼 수 있음.

이때는 null check를 하지 않는, 경계값에서의 이점이 있기 때문에 시도해보면 좋겠다.

~~공원리스트 정적이고 변하지 않기 때문에 잘 만들어 주었다.~~

~~그런데 Arrays.asList 내부의 공원 리스트 데이터가 변경될 수 있나?~~

~~하드코딩 때문에 그런 것은 아님. 개발자들도 초기 단계에서는 하드 코딩 후 TODO에서 주석 처리한다.~~

~~Arrays.asList를 써버리게 되면 target area names가 set 관련 index 넣으면 변경이 됨~~

~~따라서 결국 불변 객체가 아니게 된다.~~

- ~~따라서 List.of을 사용하는 것이 불변 객체가 되기 때문에 더 괜찮을 것 같다.~~
    

~~update public data()~~

~~스케줄러~~

~~try catch가 이중으로 되어있다.~~

- ~~상위 블록은 필요가 없을 듯.~~
    

~~하다보니까 그렇게 되었습니다 - 이유가 있다면 써도 상관없음.~~

벗어난 부분은 그냥 써도 되지 않을까?

로깅이 정말 잘 찍혀있어 좋았다. 레벨도 잘 나뉘어 있었다.

30분 주기 업데이트. 새로고침 버튼은 어떻게 협의되었는지? → 일단 없애기로 협의되었음.

문구는 30분이지만 더 일찍 올 수 있다.

변경 시점의 이벤트 저장, 클라이언트로 변경되었다는 이벤트 발송하기

통신은 websocket, sse, long/short polling

이벤트 발송 스펙은 redis pub/sub, kafka, rabbit MQ

SeoulApiClient getCityData

외부 호출 시 4xx, 5xx 에러 처리 필요할 듯.

예외 구분은 외부에서 4xx, 5xx. / 내부에서 에러

중요 외부 호출은 에러 발생 시 알림시스템 구축(차후 넣어도 됨)

외부 서버 다운 시 우리는 어떻게 데이터 던져줘야 하나?

@Setter처리

필요한 필드에만 @Setter 명시할 것

남발할 시 프로젝트가 커지면 디버깅하기 매우 어려워진다.

생성자 및 빌더 패턴 해도 되고, 각 필드별 세터 생성하는 것이 좋다.

안좋아 하는 사람도 있음.

@Data도 위험하다.

@ToString은 보안적으로 직렬화 시 데이터 노출 때문에 뺄 수 있으면 빼는 게 좋음.

프로젝트 템플릿 있는지?

없음. 회사는 레거시 많이 함.

우리는 일반적인 도메인 패키지 형식은 아님.

본인은 이 프로젝트의 구조에서 벗어나지 않음.

아키텍처가 궁금하면 카카오뱅크 쪽 헥사고날 아키텍처 패턴 찾아보면 좋음.

계정계, 비계정계 도메인이 잘 나뉘어져 있다?

[https://tech.kakaobank.com/posts/2311-hexagonal-architecture-in-messaging-hub/](https://tech.kakaobank.com/posts/2311-hexagonal-architecture-in-messaging-hub/)

웹은 여전히 세션 기반으로 함. 개발자 도구에서 토큰 노출 가능함.

모바일은 auth 좀 길게 가게 해서 그렇게 개발한다.